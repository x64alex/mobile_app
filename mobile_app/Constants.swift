import Foundation

struct Constants {
    static let domain = "localhost:2320/"
    static let webAdress = "http://" + domain
    static let webAdressAll = webAdress + "all"
    static let wsAdress = "ws://"+domain
}
